# @package _global_
data:
  module:
    _target_: medregression3d.data.datamodules.AgeReg_DataModule
    name: AgeReg
    data_root_dir: /bdm-das/ADSP_v1/nnssl_dataset/nnssl_preprocessed/Dataset001_UKBB # To be changed for each dataset
    batch_size: 4
    train_transforms:
      _target_: medregression3d.data.batchgenerators_transforms.get_training_transforms
      patch_size: ${data.patch_size}
      rotation_for_DA: 0.523599
      mirror_axes: [0,1,2]
      do_dummy_2d_data_aug: False
    test_transforms: 
      _target_: medregression3d.data.batchgenerators_transforms.get_test_transforms
      patch_size: ${data.patch_size}
      do_dummy_2d_data_aug: False
  cv:
    k: 1

  num_classes: 1
  patch_size: [160, 160, 160]

model:
  task: 'Regression'
  cifar_size: False
  input_channels: 1
  input_dim: 3
  input_shape: ${data.patch_size}
  optimizer: AdamW
  lr: 0.001
  warmstart: 20
  weight_decay: 1e-2


trainer:
  logger:
    project: nnSSL UKBB Age Regression
    name: <wandb_run_name> # This run name also apply to the run name that is being saved locally.
  accumulate_grad_batches: 48
  max_epochs: 200
  sync_batchnorm: True

metrics:
  - 'mae' 
  - 'mse'
  #'f1'
  #- 'balanced_acc'
  #- 'ap'
  #- 'auroc'