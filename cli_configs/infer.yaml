hydra:
  output_subdir: null
  job:
    chdir: False
  run:
    dir: .

override hydra/hydra_logging: disable
override hydra/job_logging: disable

exp_dir: /home/jma/projects/aaron/Dataset/nnssl_dataset/nnssl_results/Dataset003_LiverAge/ResNetL-VOCO/liver/checkpoints # path to the directory containing the hydra configs and fold directories
ckpt_dir: /home/jma/projects/aaron/Dataset/nnssl_dataset/nnssl_results/Dataset003_LiverAge/ResNetL-VOCO/liver/checkpoints/6c664b4d-9452-4fca-8ee5-a114f9114a27 # Parent directory of fold 0
fold: 0  # if no fold is given, an ensemble of all available folds will be used (if only one fold is available this can be ignored)
pred_dir: null

metrics:
  - 'mae'
  - 'mse'