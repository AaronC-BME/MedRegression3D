hydra:
  output_subdir: null
  job:
    chdir: False
  run:
    dir: .

override hydra/hydra_logging: disable
override hydra/job_logging: disable

exp_dir: /bdm-das/ADSP_v1/nnssl_dataset/nnssl_results/ADNI_ResEnc_45kNMC_DX_cls_DblWarmup_lr_1e-3/DX_cls/checkpoints # path to the directory containing the hydra configs and fold directories
ckpt_dir: /bdm-das/ADSP_v1/nnssl_dataset/nnssl_results/ADNI_ResEnc_45kNMC_DX_cls_DblWarmup_lr_1e-3/DX_cls/checkpoints/44afe32b-e333-47e7-a746-aaf7468c63bf # Parent directory of fold 0
fold: 0  # if no fold is given, an ensemble of all available folds will be used (if only one fold is available this can be ignored)
pred_dir: null

metrics:
  - 'f1'
  - 'balanced_acc'
  - 'ap'
  - 'auroc'